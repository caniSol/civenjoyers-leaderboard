[project]
name = "civenjoyers-leaderboard"
version = "0.1.0"
description = "A leaderboard Webapp which tracks the ELO of our semi-regular Civ 6 sessions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.1",
    "uvicorn>=0.52.4",
]
